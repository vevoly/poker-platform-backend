# 发牌策略设计
## 1. 发牌策略体系架构
```mermaid
graph TB
    subgraph Core[核心策略层]
        Random[RandomDealStrategy<br/>纯随机/兜底]
        Normal[NormalDistributionStrategy<br/>正态分布控制]
    end

    subgraph Experience[用户体验层]
        Rookie[RookieDealStrategy<br/>新手保护]
        Comp[CompensationStrategy<br/>连败补偿]
        WinBal[WinningBalanceStrategy<br/>连胜平衡]
        Ret[ReturnBonusStrategy<br/>回归奖励]
    end

    subgraph Commercial[商业化层]
        Vip[VipDealStrategy<br/>VIP特权]
        Evt[EventDealStrategy<br/>活动加成]
        Guar[GuaranteeDealStrategy<br/>保底策略]
        Item[ItemBoostStrategy<br/>道具加成]
    end

    subgraph AI_Layer[AI 控制层]
        AIDeal[AIDealStrategy<br/>AI难度]
        Dyn[DynamicBalanceStrategy<br/>动态平衡]
    end

%% Connections
    Normal --> Rookie
    Rookie --> Comp
    Comp --> WinBal
    WinBal --> Ret
    Ret --> Vip
    Vip --> Evt
    Evt --> Guar
    Guar --> Item
    Item --> AIDeal
    AIDeal --> Dyn
```

| 策略 (Strategy) | 功能 | 触发条件      | 大厂实践 |
| :--- | :--- |:----------| :--- |
| **NormalDistributionStrategy** | 全局概率控制 | 每局        | 腾讯专利技术 |
| **CompensationStrategy** | 连败补偿 | 连败 >=3 局  | 通用留存策略 |
| **WinningBalanceStrategy** | 连胜平衡 | 连胜 >=3 局  | 通用平衡策略 |
| **VipDealStrategy** | VIP特权 | VIP等级 >=1 | 商业化必备 |
| **ReturnBonusStrategy** | 回归奖励 | 流失 >=3 天  | 召回流失玩家 |
| **EventDealStrategy** | 活动加成 | 活动期间      | 运营活动 |
| **GuaranteeDealStrategy** | 保底策略 | 使用道具      | 道具系统 |
| **ItemBoostStrategy** | 道具加成 | 使用道具      | 道具系统 |
| **RookieDealStrategy** | 新手保护 | 新手期       | 新手引导 |
| **AIDealStrategy** | AI难度 | AI玩家      | 机器人控制 |

## 2. 发牌策略的工作原理
### 2.1 整体工作流程
```mermaid
flowchart TB
    subgraph Input[输入层]
        A1[玩家ID]
        A2[游戏类型]
        A3[历史数据<br/>连胜/连败/流失天数/VIP等级]
        A4[生效道具]
        A5[AI标识]
    end
    
    subgraph Manager[策略管理器]
        B1[策略优先级排序]
        B2[逐策略检查]
        B3[牌型决策]
    end
    
    subgraph Strategies[策略执行层]
        C1[NormalDistribution<br/>基础概率控制]
        C2[Compensation<br/>连败补偿]
        C3[WinningBalance<br/>连胜平衡]
        C4[VipDeal<br/>VIP特权]
        C5[ReturnBonus<br/>回归奖励]
        C6[EventDeal<br/>活动加成]
        C7[Guarantee<br/>保底策略]
        C8[ItemBoost<br/>道具加成]
        C9[Rookie<br/>新手保护]
        C10[AI<br/>AI难度]
    end
    
    subgraph Output[输出层]
        D1[目标牌型等级]
        D2[发牌控制器执行]
    end
    
    Input --> Manager
    Manager --> Strategies
    Strategies --> Output
    
    C1 -.->|优先级最低<br/>兜底| Manager
    C2 -.->|优先级高<br/>玩家体验| Manager
    C3 -.->|调整牌型| Manager
```
### 2.2 每种策略的详细工作原理
#### 策略1：正态分布策略 (NormalDistributionStrategy)
```mermaid
flowchart TB
    subgraph Config[牌型配置表]
        T1[单张: 25%]
        T2[对子: 20%]
        T3[顺子: 12%]
        T4[炸弹: 5%]
        T5[王炸: 1%]
    end
    
    subgraph Monitor[全局监控]
        M1[已发牌局数: 10000]
        M2[各牌型实际出现次数]
        M3[计算偏差]
    end
    
    subgraph Adjust[动态调整]
        A1{炸弹实际出现率 7%?}
        A2[理论概率 5%]
        A3[偏差 +2%]
        A4[降低炸弹选择概率]
    end
    
    subgraph Output[输出]
        O1[正态分布随机选择]
        O2[返回牌型]
    end
    
    Config --> Monitor
    Monitor --> Adjust
    Adjust --> Output
```
##### 工作原理：
1. 牌型配置表：预设每种牌型的理论概率（基于正态分布）
2. 全局监控：记录所有牌局中每种牌型的实际出现次数
3. 偏差计算：比较实际概率与理论概率的差异
4. 动态调整：实际概率过高时降低选择概率，过低时提高
5. 随机选择：使用高斯分布随机选择牌型

#### 策略2：连败补偿策略 (CompensationStrategy)
```mermaid
flowchart TB
    subgraph Input[输入]
        I1[玩家ID: 12345]
        I2[查询连败次数]
    end
    
    subgraph Storage[数据存储]
        S1[Redis/数据库]
        S2[连败计数器: 5]
    end
    
    subgraph Decision[决策逻辑]
        D1{连败次数 >= 3?}
        D2{连败次数 >= 5?}
        D3{连败次数 >= 8?}
        D4{连败次数 >= 12?}
    end
    
    subgraph Compensate[补偿等级]
        C1[基础补偿: 炸弹]
        C2[中级补偿: 王炸]
        C3[高级补偿: 王炸+]
        C4[顶级补偿: 皇家同花顺]
    end
    
    subgraph Probability[概率控制]
        P1[60%概率触发补偿]
        P2[避免100%补偿]
    end
    
    subgraph Output[输出]
        O1[返回目标牌型]
        O2[记录补偿触发]
    end
    
    Input --> Storage
    Storage --> Decision
    Decision --> Compensate
    Compensate --> Probability
    Probability --> Output
```
##### 工作原理：
1. 记录连败：每次玩家输牌，连败计数器+1；赢牌时归零
2. 阈值判断：3/5/8/12 连败分别触发不同等级补偿
3. 概率触发：不是100%补偿，连败越多触发概率越高
4. 牌型提升：根据连败等级给予对应强度的牌型

#### 策略3：连胜平衡策略 (WinningBalanceStrategy)
```mermaid
flowchart TB
    subgraph Input[输入]
        I1[原始牌型: 炸弹]
        I2[连胜次数: 5]
    end
    
    subgraph Decision[决策]
        D1{连胜 >= 3?}
        D2{连胜 >= 5?}
        D3{连胜 >= 8?}
    end
    
    subgraph Balance[平衡强度]
        B1[轻度: 降低30%]
        B2[中度: 降低50%]
        B3[高度: 降低70%]
    end
    
    subgraph Downgrade[牌型降级]
        G1[炸弹 → 顺子]
        G2[王炸 → 炸弹]
    end
    
    subgraph Output[输出]
        O1[调整后牌型]
    end
    
    Input --> Decision
    Decision --> Balance
    Balance --> Downgrade
    Downgrade --> Output
```
##### 工作原理：
1. 记录连胜：每次玩家赢牌，连胜计数器+1；输牌时归零
2. 平衡触发：连胜达到阈值后触发平衡
3. 强度计算：根据连胜次数计算平衡强度系数
4. 牌型降级：将原始牌型降低指定等级

#### 策略4：VIP特权策略 (VipDealStrategy)
```mermaid
flowchart TB
    subgraph Input[输入]
        I1[玩家ID]
        I2[VIP等级: 5]
    end
    
    subgraph VipConfig[VIP配置]
        V1[V1: +5% 概率]
        V2[V2: +10% 概率]
        V3[V5: +25% 概率]
        V4[V9: +50% 概率]
    end
    
    subgraph Decision[决策]
        D1{触发VIP加成?}
        D2{VIP等级 >= 7?}
    end
    
    subgraph Special[专属牌型]
        S1[V7+: 专属牌型池]
        S2[皇家同花顺]
        S3[五小牛]
    end
    
    subgraph Output[输出]
        O1[提升后牌型]
    end
    
    Input --> VipConfig
    VipConfig --> Decision
    Decision --> Special
    Special --> Output
```
##### 工作原理：
1. VIP等级查询：获取玩家当前VIP等级
2. 加成计算：根据等级计算加成系数（5%-50%）
3. 概率判定：随机判定是否触发VIP加成
4. 专属牌型：高等级VIP可触发专属牌型池

#### 策略5：回归奖励策略 (ReturnBonusStrategy)
```mermaid
flowchart TB
    subgraph Input[输入]
        I1[玩家ID]
        I2[最后登录时间: 2024-01-01]
        I3[当前时间: 2024-01-20]
    end
    
    subgraph Calculate[计算]
        C1[流失天数 = 19天]
    end
    
    subgraph Threshold[阈值判断]
        T1{流失 >= 3天?}
        T2{流失 >= 7天?}
        T3{流失 >= 15天?}
        T4{流失 >= 30天?}
    end
    
    subgraph Bonus[奖励等级]
        B1[基础回归: 炸弹]
        B2[中级回归: 王炸]
        B3[高级回归: 同花顺]
        B4[顶级回归: 皇家同花顺]
    end
    
    subgraph Limit[奖励限制]
        L1[仅限前10局]
        L2[每局消耗1次机会]
    end
    
    subgraph Output[输出]
        O1[回归奖励牌型]
    end
    
    Input --> Calculate
    Calculate --> Threshold
    Threshold --> Bonus
    Bonus --> Limit
    Limit --> Output
```
##### 工作原理：
1. 流失计算：当前时间 - 最后登录时间 = 流失天数
2. 阈值判断：3/7/15/30天分别对应不同奖励等级
3. 奖励次数：回归后前N局享受奖励（如10局）
4. 次数消耗：每局消耗一次奖励机会

#### 策略6：活动加成策略 (EventDealStrategy)
```mermaid
flowchart TB
    subgraph Input[输入]
        I1[活动ID: SpringFestival]
        I2[活动配置]
    end
    
    subgraph Config[活动配置]
        C1[活动时间: 2月1日-2月15日]
        C2[加成系数: +20%]
        C3[目标牌型: 炸弹/王炸]
    end
    
    subgraph Check[检查]
        D1{活动进行中?}
        D2{玩家参与活动?}
    end
    
    subgraph Boost[加成执行]
        B1[提高高牌型概率]
        B2[活动专属牌型]
    end
    
    subgraph Output[输出]
        O1[活动加成牌型]
    end
    
    Input --> Config
    Config --> Check
    Check --> Boost
    Boost --> Output
```
##### 工作原理：
1. 活动配置：后台配置活动时间、加成系数、目标牌型
2. 资格检查：确认活动进行中且玩家已参与
3. 概率提升：提高高牌型出现概率
4. 专属内容：可能解锁活动专属牌型

#### 策略7：保底策略 (GuaranteeDealStrategy)
```mermaid
flowchart TB
    subgraph Input[输入]
        I1[玩家ID]
        I2[保底等级: 牛牛]
    end
    
    subgraph Trigger[触发条件]
        T1[使用保底卡道具]
        T2[连败保底机制]
        T3[活动保底]
    end
    
    subgraph Execute[执行]
        E1[尝试生成目标牌型]
        E2[最大尝试次数: 50]
        E3[失败则降级处理]
    end
    
    subgraph Output[输出]
        O1[保底牌型]
    end
    
    Input --> Trigger
    Trigger --> Execute
    Execute --> Output
```
##### 工作原理：
1. 触发条件：道具使用、连败保底、活动保底
2. 目标设定：设定保底牌型等级（如至少牛牛）
3. 尝试生成：发牌时反复尝试直到达到目标 
4. 降级处理：超过尝试次数则返回最接近的牌型

#### 策略8：道具加成策略 (ItemBoostStrategy)
```mermaid
flowchart TB
    subgraph Input[输入]
        I1[玩家ID]
        I2[生效道具列表]
    end
    
    subgraph Items[道具类型]
        I1[好运卡: +20%]
        I2[如意卡: +15%]
        I3[双倍卡: +30%]
    end
    
    subgraph Stack[叠加计算]
        S1[总加成 = 各道具加成之和]
    end
    
    subgraph Execute[执行]
        E1{触发概率判定}
        E2[提升高牌型概率]
    end
    
    subgraph Output[输出]
        O1[加成后牌型]
    end
    
    Input --> Items
    Items --> Stack
    Stack --> Execute
    Execute --> Output
```
##### 工作原理：
1. 道具查询：获取玩家生效中的道具列表 
2. 加成叠加：多个道具的加成系数可叠加 
3. 概率判定：根据总加成计算触发概率 
4. 效果执行：触发后提升高牌型出现概率

#### 策略9：新手保护策略 (RookieDealStrategy)
```mermaid
flowchart TB
    subgraph Input[输入]
        I1[玩家ID]
        I2[注册时间]
        I3[对局数]
    end
    
    subgraph Condition[条件判断]
        C1{注册 < 7天?}
        C2{对局数 < 50?}
    end
    
    subgraph Execute[执行]
        E1[提高基础牌型]
        E2[降低对手AI难度]
    end
    
    subgraph Output[输出]
        O1[新手友好牌型]
    end
    
    Input --> Condition
    Condition --> Execute
    Execute --> Output
```
##### 工作原理：
1. 新手判定：注册时间<7天 或 对局数<50 
2. 保护执行：提高新手的基础牌型质量 
3. 保护期限：新手期结束后自动失效

#### 策略10：AI难度策略 (AIDealStrategy)
```mermaid
flowchart TB
    subgraph Input[输入]
        I1[AI玩家标识]
        I2[难度等级: 1-10]
    end
    
    subgraph Difficulty[难度映射]
        D1[难度1-2: 单张/对子]
        D2[难度3-4: 顺子]
        D3[难度5-6: 炸弹]
        D4[难度7-8: 王炸]
        D5[难度9-10: 皇家同花顺]
    end
    
    subgraph Execute[执行]
        E1[根据难度选择牌型池]
        E2[控制AI牌型强度]
    end
    
    subgraph Output[输出]
        O1[AI目标牌型]
    end
    
    Input --> Difficulty
    Difficulty --> Execute
    Execute --> Output
```
##### 工作原理：
1. 难度配置：AI难度分为1-10级 
2. 牌型映射：不同难度对应不同的牌型池 
3. 强度控制：难度越高，AI获得好牌的概率越大

## 3. 数据库设计
### 3.1 架构图
```mermaid
flowchart TB
    subgraph Game[游戏逻辑服]
        G1[game-doudizhu]
        G2[game-texas]
    end

    subgraph Service[业务服务]
        S1[service-user<br/>用户数据服务]
        S2[service-item<br/>道具服务]
        S3[service-activity<br/>活动服务]
    end

    subgraph Storage[存储层]
        DB[(MySQL<br/>持久化)]
        Cache[(Redis<br/>热数据)]
    end

    subgraph Flow[数据流程]
        F1[1. 游戏服请求发牌]
        F2[2. 调用 service-user 获取玩家数据]
        F3[3. 调用 service-item 获取道具]
        F4[4. 组装 DealContext]
        F5[5. 调用策略获取目标牌型]
        F6[6. 执行发牌]
    end

    Service --> Storage
    Game --> Service
    Game --> Flow
```
### 3.2 核心原则
1. game-common：只包含无状态的接口定义、枚举、纯算法 
2. 策略类接收参数而非主动查询数据 
3. 游戏逻辑服负责组装数据并调用策略

## 4. 牌型池
### 4.1 牌型池在游戏中的价值
```mermaid
graph TB
    subgraph Core[牌型池核心价值]
        A1[统一管理<br/>所有牌型配置]
        A2[动态调整<br/>运行时修改]
        A3[可扩展性<br/>新增游戏/玩法]
        A4[可观测性<br/>统计/分析]
    end
    
    subgraph Usage[使用方]
        B1[AI系统]
        B2[发牌控制器]
        B3[活动系统]
        B4[道具系统]
        B5[匹配系统]
    end
    
    subgraph Benefits[带来的好处]
        C1[配置化运营]
        C2[快速迭代]
        C3[AB测试]
        C4[数据驱动]
    end
    
    Core --> Usage
    Usage --> Benefits
```
### 4.2 应用场景

| 场景 | 是否需要牌型池 | 原因 |
| :--- | :---: | :--- |
| **AI难度控制** | ✅ 核心 | 不同难度等级需对应不同权重的牌型池（如：困难级AI高概率获得“三条”或“四条”） |
| **VIP特权** | ✅ 核心 | 根据VIP等级动态调整高阶牌型（如：同花、顺子）在牌型池中的出现概率 |
| **活动加成** | ✅ 核心 | 活动期间通过临时替换或增强牌型池，提升特定牌型的全局产出 |
| **新手保护** | ✅ 核心 | 使用简化或高胜率牌型池，确保新手玩家在初期获得较好的心流体验 |
| **道具加成** | ✅ 核心 | 玩家使用道具后，系统临时提升该玩家在牌型池中获取高分牌的权重 |
| **连败补偿** | ⚠️ 可选 | 既可基于牌型池强制发好牌，也可通过调整现有发牌算法的权重实现 |
| **正态分布** | ✅ 核心 | 基础概率分布本身就是通过“标准牌型池”来确保全局胜率符合预期 |

### 4.3 可扩展的牌型池系统
#### 设计原则
1. 配置驱动：牌型池通过配置文件/数据库定义
2. 运行时可修改：支持热更新 
3. 支持继承/组合：可基于基础池创建变体 
4. 多游戏扩展：新增游戏只需添加配置
#### 完整架构
```mermaid
graph TB
    subgraph Config[配置层]
        C1[牌型池定义文件<br/>handrank-pools.json]
        C2[游戏-牌型池映射<br/>game-pool-mapping.json]
        C3[权重调整规则<br/>weight-adjustment.json]
    end
    
    subgraph Core[核心层]
        P1[HandRankPoolManager<br/>管理器]
        P2[HandRankPool<br/>牌型池]
        P3[WeightCalculator<br/>权重计算器]
        P4[PoolSelector<br/>池选择器]
    end
    
    subgraph Strategy[策略层]
        S1[AIDealStrategy]
        S2[VipDealStrategy]
        S3[EventDealStrategy]
        S4[ItemBoostStrategy]
    end
    
    subgraph Runtime[运行时]
        R1[动态权重调整]
        R2[AB测试]
        R3[统计上报]
    end
    
    Config --> Core
    Core --> Strategy
    Strategy --> Runtime
```
